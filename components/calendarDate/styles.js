import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    // flex: 1,
    backgroundColor: '#fff',
    width: 67,
    height: 74,
    justifyContent: 'center',
    alignItems: 'center',
  },
  dateView: {
    height: '55%',
  },
  dateText: {
    fontSize: 30,
  },
  dayView: {
    borderTopWidth: 1,
    width: '100%',
    borderColor: '#ccc',
  },
  dayText: {
    textAlign: 'center',
  },
});
