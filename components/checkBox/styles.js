import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    gap: 10,
    alignItems: 'center',
    marginVertical: '2%',
  },
  box: {
    borderWidth: 0.6,
    backgroundColor: '#F9FAFC',
    borderRadius: 3,
  },
});
